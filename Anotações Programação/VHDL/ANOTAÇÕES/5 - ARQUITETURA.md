Essa parte é a que vem abaixo das definições de entradas e saídas, é a parte que faz o código funcionar. É onde ficam as lógicas e as funções do programa

`architecture "nome_qualquer_architecture" of "nome_qualquer_da_entity" is`
	`begin`
		`out1 <= in1 and in2;`
`end "nome_qualquer_architecture";`