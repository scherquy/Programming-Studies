O toString é basicamente utilizado para printar coisas na tela sem ficar escrevendo várias vezes em vários System.out.println diferentes, o toString é sempre publico e sempre deve escrever assim

`@Override`
`public String toString() {`
    `Sua lógica aqui`
    `return "texto";`
`}`